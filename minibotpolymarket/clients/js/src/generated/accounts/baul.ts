/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getOrdenDecoder,
  getOrdenEncoder,
  type Orden,
  type OrdenArgs,
} from "../types";

export const BAUL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  148, 76, 133, 251, 198, 51, 9, 90,
]);

export function getBaulDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BAUL_DISCRIMINATOR);
}

export type Baul = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  nombre: string;
  saldo: bigint;
  totalOrdenes: number;
  historial: Array<Orden>;
};

export type BaulArgs = {
  owner: Address;
  nombre: string;
  saldo: number | bigint;
  totalOrdenes: number;
  historial: Array<OrdenArgs>;
};

/** Gets the encoder for {@link BaulArgs} account data. */
export function getBaulEncoder(): Encoder<BaulArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["nombre", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["saldo", getU64Encoder()],
      ["totalOrdenes", getU16Encoder()],
      ["historial", getArrayEncoder(getOrdenEncoder())],
    ]),
    (value) => ({ ...value, discriminator: BAUL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Baul} account data. */
export function getBaulDecoder(): Decoder<Baul> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["nombre", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["saldo", getU64Decoder()],
    ["totalOrdenes", getU16Decoder()],
    ["historial", getArrayDecoder(getOrdenDecoder())],
  ]);
}

/** Gets the codec for {@link Baul} account data. */
export function getBaulCodec(): Codec<BaulArgs, Baul> {
  return combineCodec(getBaulEncoder(), getBaulDecoder());
}

export function decodeBaul<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Baul, TAddress>;
export function decodeBaul<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Baul, TAddress>;
export function decodeBaul<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Baul, TAddress> | MaybeAccount<Baul, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBaulDecoder(),
  );
}

export async function fetchBaul<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Baul, TAddress>> {
  const maybeAccount = await fetchMaybeBaul(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBaul<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Baul, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBaul(maybeAccount);
}

export async function fetchAllBaul(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Baul>[]> {
  const maybeAccounts = await fetchAllMaybeBaul(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBaul(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Baul>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBaul(maybeAccount));
}
