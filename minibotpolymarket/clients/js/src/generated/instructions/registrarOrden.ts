/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BOT_POLYMARKET_PROGRAM_ADDRESS } from "../programs";

export const REGISTRAR_ORDEN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [198, 143, 218, 36, 251, 251, 105, 185],
);

export function getRegistrarOrdenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTRAR_ORDEN_DISCRIMINATOR,
  );
}

export type RegistrarOrdenInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegistrarOrdenInstructionData = {
  discriminator: ReadonlyUint8Array;
  idMercado: string;
  monto: bigint;
  tipoMov: number;
};

export type RegistrarOrdenInstructionDataArgs = {
  idMercado: string;
  monto: number | bigint;
  tipoMov: number;
};

export function getRegistrarOrdenInstructionDataEncoder(): Encoder<RegistrarOrdenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["idMercado", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["monto", getU64Encoder()],
      ["tipoMov", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTRAR_ORDEN_DISCRIMINATOR }),
  );
}

export function getRegistrarOrdenInstructionDataDecoder(): Decoder<RegistrarOrdenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["idMercado", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["monto", getU64Decoder()],
    ["tipoMov", getU8Decoder()],
  ]);
}

export function getRegistrarOrdenInstructionDataCodec(): Codec<
  RegistrarOrdenInstructionDataArgs,
  RegistrarOrdenInstructionData
> {
  return combineCodec(
    getRegistrarOrdenInstructionDataEncoder(),
    getRegistrarOrdenInstructionDataDecoder(),
  );
}

export type RegistrarOrdenInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  idMercado: RegistrarOrdenInstructionDataArgs["idMercado"];
  monto: RegistrarOrdenInstructionDataArgs["monto"];
  tipoMov: RegistrarOrdenInstructionDataArgs["tipoMov"];
};

export function getRegistrarOrdenInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
>(
  input: RegistrarOrdenInput<
    TAccountOwner,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegistrarOrdenInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BOT_POLYMARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegistrarOrdenInstructionDataEncoder().encode(
      args as RegistrarOrdenInstructionDataArgs,
    ),
    programAddress,
  } as RegistrarOrdenInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedRegistrarOrdenInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: RegistrarOrdenInstructionData;
};

export function parseRegistrarOrdenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegistrarOrdenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegistrarOrdenInstructionDataDecoder().decode(instruction.data),
  };
}
