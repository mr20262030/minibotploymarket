/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BOT_POLYMARKET_PROGRAM_ADDRESS } from "../programs";

export const DEPOSITAR_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  207, 16, 199, 114, 220, 5, 105, 219,
]);

export function getDepositarDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DEPOSITAR_DISCRIMINATOR);
}

export type DepositarInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositarInstructionData = {
  discriminator: ReadonlyUint8Array;
  monto: bigint;
};

export type DepositarInstructionDataArgs = { monto: number | bigint };

export function getDepositarInstructionDataEncoder(): FixedSizeEncoder<DepositarInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["monto", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DEPOSITAR_DISCRIMINATOR }),
  );
}

export function getDepositarInstructionDataDecoder(): FixedSizeDecoder<DepositarInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["monto", getU64Decoder()],
  ]);
}

export function getDepositarInstructionDataCodec(): FixedSizeCodec<
  DepositarInstructionDataArgs,
  DepositarInstructionData
> {
  return combineCodec(
    getDepositarInstructionDataEncoder(),
    getDepositarInstructionDataDecoder(),
  );
}

export type DepositarInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  monto: DepositarInstructionDataArgs["monto"];
};

export function getDepositarInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
>(
  input: DepositarInput<TAccountOwner, TAccountVault, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): DepositarInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BOT_POLYMARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDepositarInstructionDataEncoder().encode(
      args as DepositarInstructionDataArgs,
    ),
    programAddress,
  } as DepositarInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedDepositarInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: DepositarInstructionData;
};

export function parseDepositarInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDepositarInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDepositarInstructionDataDecoder().decode(instruction.data),
  };
}
