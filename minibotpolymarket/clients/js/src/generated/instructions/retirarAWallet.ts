/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BOT_POLYMARKET_PROGRAM_ADDRESS } from "../programs";

export const RETIRAR_A_WALLET_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([56, 135, 36, 187, 223, 121, 79, 183]);

export function getRetirarAWalletDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RETIRAR_A_WALLET_DISCRIMINATOR,
  );
}

export type RetirarAWalletInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountCuentaDestino extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountCuentaDestino extends string
        ? WritableAccount<TAccountCuentaDestino>
        : TAccountCuentaDestino,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RetirarAWalletInstructionData = {
  discriminator: ReadonlyUint8Array;
  monto: bigint;
};

export type RetirarAWalletInstructionDataArgs = { monto: number | bigint };

export function getRetirarAWalletInstructionDataEncoder(): FixedSizeEncoder<RetirarAWalletInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["monto", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RETIRAR_A_WALLET_DISCRIMINATOR }),
  );
}

export function getRetirarAWalletInstructionDataDecoder(): FixedSizeDecoder<RetirarAWalletInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["monto", getU64Decoder()],
  ]);
}

export function getRetirarAWalletInstructionDataCodec(): FixedSizeCodec<
  RetirarAWalletInstructionDataArgs,
  RetirarAWalletInstructionData
> {
  return combineCodec(
    getRetirarAWalletInstructionDataEncoder(),
    getRetirarAWalletInstructionDataDecoder(),
  );
}

export type RetirarAWalletInput<
  TAccountOwner extends string = string,
  TAccountCuentaDestino extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  cuentaDestino: Address<TAccountCuentaDestino>;
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  monto: RetirarAWalletInstructionDataArgs["monto"];
};

export function getRetirarAWalletInstruction<
  TAccountOwner extends string,
  TAccountCuentaDestino extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
>(
  input: RetirarAWalletInput<
    TAccountOwner,
    TAccountCuentaDestino,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RetirarAWalletInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountCuentaDestino,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BOT_POLYMARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    cuentaDestino: { value: input.cuentaDestino ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("cuentaDestino", accounts.cuentaDestino),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRetirarAWalletInstructionDataEncoder().encode(
      args as RetirarAWalletInstructionDataArgs,
    ),
    programAddress,
  } as RetirarAWalletInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCuentaDestino,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedRetirarAWalletInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    cuentaDestino: TAccountMetas[1];
    vault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RetirarAWalletInstructionData;
};

export function parseRetirarAWalletInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRetirarAWalletInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      cuentaDestino: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRetirarAWalletInstructionDataDecoder().decode(instruction.data),
  };
}
