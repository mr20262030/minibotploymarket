/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BOT_POLYMARKET_PROGRAM_ADDRESS } from "../programs";

export const INICIAR_BAUL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  157, 73, 202, 45, 141, 161, 141, 22,
]);

export function getIniciarBaulDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INICIAR_BAUL_DISCRIMINATOR,
  );
}

export type IniciarBaulInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableSignerAccount<TAccountVault> &
            AccountSignerMeta<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type IniciarBaulInstructionData = {
  discriminator: ReadonlyUint8Array;
  nombre: string;
};

export type IniciarBaulInstructionDataArgs = { nombre: string };

export function getIniciarBaulInstructionDataEncoder(): Encoder<IniciarBaulInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nombre", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INICIAR_BAUL_DISCRIMINATOR }),
  );
}

export function getIniciarBaulInstructionDataDecoder(): Decoder<IniciarBaulInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nombre", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getIniciarBaulInstructionDataCodec(): Codec<
  IniciarBaulInstructionDataArgs,
  IniciarBaulInstructionData
> {
  return combineCodec(
    getIniciarBaulInstructionDataEncoder(),
    getIniciarBaulInstructionDataDecoder(),
  );
}

export type IniciarBaulInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: TransactionSigner<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  nombre: IniciarBaulInstructionDataArgs["nombre"];
};

export function getIniciarBaulInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
>(
  input: IniciarBaulInput<TAccountOwner, TAccountVault, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): IniciarBaulInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BOT_POLYMARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getIniciarBaulInstructionDataEncoder().encode(
      args as IniciarBaulInstructionDataArgs,
    ),
    programAddress,
  } as IniciarBaulInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedIniciarBaulInstruction<
  TProgram extends string = typeof BOT_POLYMARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: IniciarBaulInstructionData;
};

export function parseIniciarBaulInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedIniciarBaulInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getIniciarBaulInstructionDataDecoder().decode(instruction.data),
  };
}
